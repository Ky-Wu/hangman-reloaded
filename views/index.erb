<!DOCTYPE html>
<html>
  <head>
    <title>Hangman</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/hangman.css" />
    <link href="https://fonts.googleapis.com/css?family=Arvo|Lobster+Two" rel="stylesheet">
  </head>
  <body>
    <h1>Hangman</h1>
    <p><%= message %></p>
    <p><%= display.join(" ") %></p>
    <p><%= countdown %> turns remaining.</p>
    <form>
      <input type='text' name='guess' />
      <input type='submit' value="Guess!" />
    </form>
    <p>Incorrect letters:</p>
    <p><%= incorrect_letters.join("  ") %> </p>
    <p><%= hangmen_won %>
      <% if hangmen_won == 1 %>
      game
      <% else %>
      games
      <% end %>
    won this server lifetime. </p>
  </body>
</html>

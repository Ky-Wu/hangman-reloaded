<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <p><%= message %></p>
    <p><%= word %> <%= display.join(" ") %></p>
    <p><%= countdown %> turns remaining.</p>
    <form>
      <input type='text' name='guess' />
      <input type='submit' value="Guess!" />
    </form>
    <p>Incorrect letters: <%= incorrect_letters.join("  ") %> </p>
  </body>
</html>
